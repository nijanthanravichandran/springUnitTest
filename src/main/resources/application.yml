spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/db"
    username: "user"
    password: "admin"
  jpa:
    show-sql: true
    hibernate:
        ddl-auto: "update"
    properties:
      hibernate:
        dialect: "org.hibernate.dialect.MySQL8Dialect"
        format_sql: true